# Axolync Spotify Addon

Seed-first home for the Spotify SongMetadata addon.

Current scope:

- one Spotify-backed SongMetadata adapter candidate
- stage 1 focus on `canonicalDurationMs`
- implementation is expected to wrap the maintained official Spotify TypeScript SDK rather than re-implement Spotify Web API calls by hand

This repo does not ship runtime code yet.
